
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global helpers
    function isSignedIn() { return request.auth != null; }
    
    // Check if the user is an admin by checking the 'admins' collection
    function isAdmin() { 
      return isSignedIn() && exists(/databases/$(database)/documents/admins/$(request.auth.uid)); 
    }

    match /users/{userId} {
      allow get: if isSignedIn();
      allow list: if isAdmin();
      allow create, update: if isSignedIn() && request.auth.uid == userId;
      allow delete: if isAdmin();
    }

    match /admins/{adminId} {
      allow get: if isSignedIn() && request.auth.uid == adminId;
    }

    match /salons/{salonId} {
      allow read: if true;
      // Reverting to base ownership rule
      allow write: if isSignedIn() && (
        isAdmin() || 
        salonId == 'salon_' + request.auth.uid ||
        (request.resource.data.ownerId == request.auth.uid && (resource == null || resource.data.ownerId == request.auth.uid))
      );

      match /services/{serviceId} {
        allow read: if true;
        allow write: if isAdmin() || (isSignedIn() && get(/databases/$(database)/documents/salons/$(salonId)).data.ownerId == request.auth.uid);
      }
    }

    match /bookings/{bookingId} {
      // Base check for single-doc reads. List queries often fail here in production.
      allow read: if isSignedIn() && (
        isAdmin() ||
        resource.data.userId == request.auth.uid || 
        resource.data.salonOwnerId == request.auth.uid
      );
      
      allow create: if isSignedIn() && 
        request.resource.data.userId == request.auth.uid;
      
      allow update: if isSignedIn() && (
        isAdmin() ||
        resource.data.userId == request.auth.uid || 
        resource.data.salonOwnerId == request.auth.uid
      );

      allow delete: if isAdmin();
    }
  }
}
